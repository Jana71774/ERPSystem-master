@model ERPSystem.Models.Spec
@{
    ViewData["Title"] = "Edit Specification";
    Layout = "_Layout";
}

<style>
    .erp-form {
        max-width: 600px;
        margin: auto;
        border: 2px solid #2da2ff;
        border-radius: 6px;
        background: #f9fbfd;
        padding: 20px;
    }
</style>

<div class="container mt-4">
    <div class="erp-form">

        <div class="d-flex justify-content-between mb-3">
            <h6>Edit Specification</h6>

            <!-- 🔙 BACK -->
            <button type="button"
                    class="btn btn-sm btn-secondary"
                    onclick="history.back();">
                ← Back
            </button>
        </div>

        <form asp-action="Edit" method="post">

            <input type="hidden" asp-for="ItemCode" />
            <input type="hidden" asp-for="ItemName" />
            <input type="hidden" asp-for="SpecId" />

            <div class="mb-3">
                <label class="form-label">Item Code</label>
                <input class="form-control" value="@Model.ItemCode" readonly />
            </div>

            <div class="mb-3">
                <label class="form-label">Spec ID</label>
                <input class="form-control" value="@Model.SpecId" readonly />
            </div>

            <div class="mb-3">
                <label class="form-label">Spec Data</label>
                <input asp-for="SpecData" class="form-control" required />
            </div>

            <div class="mb-3">
                <label class="form-label">Spec Value</label>
                <input asp-for="SpecValue" class="form-control" required />
            </div>

            <div class="text-end">
                <button class="btn btn-sm btn-primary">
                    Update
                </button>
            </div>

        </form>

    </div>
</div>
